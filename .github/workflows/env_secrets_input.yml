name: Demo CI Env Secrets
on: 
  push:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: say hello
        run: echo "Hello Shashank from second yml file!"
    
      - name: Install
        env: 
          APP_ENV: demo
        run: echo "Hello Shashank, Your environment is $APP_ENV"